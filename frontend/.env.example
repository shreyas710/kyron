# Gemini model used by the frontend chat integration
VITE_GEMINI_MODEL=gemini-1.5-flash

# WARNING: using Gemini directly from frontend exposes this key in the browser.
# Use only for quick demo/prototyping. For production, route through backend.
VITE_GEMINI_API_KEY=your_gemini_api_key_here
