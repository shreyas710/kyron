[build]
  command = "npm run build"
  publish = "dist"

# Proxy /api requests to your EC2 Backend
[[redirects]]
  from = "/api/*"
  to = "https://your-ec2-domain.com/api/:splat"
  status = 200
  force = true