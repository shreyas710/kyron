[build]
  command = "npm run build"
  publish = "dist"

# Proxy /api requests to your EC2 Backend
[[redirects]]
  from = "/api/*"
  to = "http://13.57.14.110:8787/api/:splat"
  status = 200
  force = true